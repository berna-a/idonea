# Convex (gerado automaticamente por `npx convex dev` em .env.local — este ficheiro é só referência)
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Mapbox (mapa de imóveis em /properties — token público, pk.*)
VITE_MAPBOX_TOKEN=
